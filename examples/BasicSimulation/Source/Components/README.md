# Components

BasicSimulation currently declares its data-only object properties in
`../BasicSimulationTypes.h` and `BasicSimulationRuntime::CreateObjectTypes`.
New component declarations belong in this directory.
